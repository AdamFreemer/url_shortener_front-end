// import { renderHook, act } from '@testing-library/react-hooks';
import { LinkContext } from '../LinkContext';

// describe('<LinkContext />', () => {
//   it('does stuff', () => {
//     const { result } = renderHook(() =>
//       LinkContext({
//         user: UsersMock[0],
//         workSchedule: workScheduleMock,
//         workScheduleTypeId: WorkScheduleTypesMock[0],
//       }));

//     expect(result.current.props.value.workScheduleTime(1, workScheduleMock)).toBeDefined();
//     });
// });

